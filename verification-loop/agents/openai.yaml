interface:
  display_name: "Verification Loop"
  short_description: "Select and interpret checks that prove behavior"
  default_prompt: "Use $verification-loop to verify this change against its acceptance criteria and report any gaps."
